services:
  - type: web
    name: jenisha-admin
    env: static
    rootDir: admn
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
